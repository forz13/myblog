{# src/Blogger/BlogBundle/Resources/views/Page/index.html.twig #}
{% extends 'BloggerBlogBundle::layout.html.twig' %}

{#{% block body %}#}
    {#{% for blog in blogs %}#}
        {#<article class="blog">#}
            {#<div class="date">#}
                {#<time datetime="{{ blog.created|date('c') }}">{{ blog.created|date('l, F j, Y') }}</time>#}
            {#</div>#}
            {#<header>#}
                {#<h2><a href="{{ path('BloggerBlogBundle_blog_show', { 'id': blog.id }) }}">{{ blog.title }}</a></h2>#}
            {#</header>#}
            {#{% if blog.image %}#}
                {#<img src="{{ asset(['images/', blog.image]|join) }}"/>#}
            {#{% endif %}#}
            {#<div class="snippet">#}
                {#<p>{{ blog.blog(500) | raw }}</p>#}
                {#<p class="continue"><a href="{{ path('BloggerBlogBundle_blog_show', { 'id': blog.id }) }}">Continue#}
                        {#reading...</a></p>#}
            {#</div>#}

            {#<footer class="meta">#}
                {#<p>Comments: -</p>#}
                {#<p>Posted by <span class="highlight">{{ blog.author }}</span> at {{ blog.created|date('h:iA') }}</p>#}
                {#<p>Tags: <span class="highlight">{{ blog.tags }}</span></p>#}
            {#</footer>#}
        {#</article>#}
    {#{% else %}#}
        {#<p>There are no blog entries for symblog</p>#}
    {#{% endfor %}#}
{#{% endblock %}#}
{% block body %}
    <div class="about">
        <div class="container">
            <div class="about-main">
                <div class="col-md-8 about-left">
                    {% if blogs[0] is defined %}
                        <div class="about-two">
                            <h2>
                                <a href="{{ path('BloggerBlogBundle_blog_show', { 'id': blogs[0].id }) }}">{{ blogs[0].title }}</a>
                            </h2>
                            {% if blogs[0].image is defined %}
                                <a href="{{ path('BloggerBlogBundle_blog_show', { 'id': blogs[0].id }) }}"><img
                                            src="{{ asset(['images/', blogs[0].image]|join) }}"/></a>
                            {% endif %}
                            {{ blogs[0].blog(500) | slice(0, 500) | raw }}...
                            <p>Posted by {{ blogs[0].author }} on {{ blogs[0].created|date('j F,Y') }}
                                <a href="{{ path('BloggerBlogBundle_blog_show', { 'id': blogs[0].id }) }}">comments({{ blogs[0].comments|length }}
                                    )</a>
                            </p>
                            <div class="about-btn">
                                <a href="{{ path('BloggerBlogBundle_blog_show', { 'id': blogs[0].id }) }}">Read More</a>
                            </div>
                            <ul>
                                <li><p>Share : </p></li>
                                <li><a href="#"><span class="fb"> </span></a></li>
                                <li><a href="#"><span class="twit"> </span></a></li>
                                <li><a href="#"><span class="pin"> </span></a></li>
                                <li><a href="#"><span class="rss"> </span></a></li>
                                <li><a href="#"><span class="drbl"> </span></a></li>
                            </ul>
                        </div>
                    {% endif %}
                    {% set popBlog=blogs|slice(1) %}
                    <div class="about-tre">
                        {% for  blogMinilist in popBlog|batch(2) %}
                            <div class="row match-my-cols">
                                {% for blog in blogMinilist %}
                                    <div class="col-md-6 abt-left">
                                        <h3>
                                            <a href="{{ path('BloggerBlogBundle_blog_show', { 'id': blog.id }) }}">{{ blog.title }}</a>
                                        </h3>
                                        {% if blog.image is defined %}
                                            <a href="{{ path('BloggerBlogBundle_blog_show', { 'id': blog.id }) }}"><img
                                                        src="{{ asset(['images/', blog.image]|join) }}" alt=""/></a>
                                        {% endif %}
                                        {{ blog.blog|slice(0, 300) | raw }}
                                        <p>
                                            <label><a href="{{ path('BloggerBlogBundle_blog_show', { 'id': blogs[0].id }) }}">Read
                                                    More</a></label>
                                        </p>
                                    </div>
                                {% endfor %}
                            </div>
                        {% endfor %}
                    </div>
                </div>
                <div class="col-md-4 about-right heading">
                    <div class="abt-1">
                        <h3>CATEGORIES</h3>
                    </div>
                    <div class="abt-2">
                        <h3>MOST COMMENTED</h3>
                        {% for topBlog in topBlogs %}
                            <div class="might-grid">
                                <div class="might-top">
                                    <h4>
                                        <a href="{{ path('BloggerBlogBundle_blog_show', { 'id': topBlog.id }) }}">{{ topBlog.title }}</a>
                                    </h4>
                                    <p>{{ topBlog.blog|slice(0, 50) | raw }}...</p>
                                </div>
                                <div class="clearfix"></div>
                            </div>
                        {% endfor %}
                    </div>
                    <div class="abt-2">
                        <h3>ARCHIVES</h3>
                        <ul>
                            {% for blog in blogs|reverse %}
                                <li>
                                    <a href="{{ path('BloggerBlogBundle_blog_show', { 'id': blog.id }) }}">{{ blog.title }}</a>
                                </li>
                            {% endfor %}
                        </ul>
                    </div>
                </div>
                <div class="clearfix"></div>
            </div>
        </div>
    </div>
{% endblock %}